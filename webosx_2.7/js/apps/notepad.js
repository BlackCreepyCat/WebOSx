        // Traductions propres à l'app Bloc-notes (auto-suffisantes, n'impactent pas js/core/lang/*.js)
        I18N.registerLang('fr', {
            'app.notepad.placeholder': 'Tapez votre texte ici...',
            'notepad.menu.file': 'Fichier',
            'notepad.menu.edit': 'Édition',
            'notepad.menu.format': 'Format',
            'notepad.menu.help': 'Aide',
            'notepad.file.new': 'Nouveau',
            'notepad.file.open': 'Ouvrir...',
            'notepad.file.save': 'Enregistrer sous...',
            'notepad.edit.undo': 'Annuler',
            'notepad.edit.redo': 'Rétablir',
            'notepad.edit.cut': 'Couper',
            'notepad.edit.copy': 'Copier',
            'notepad.edit.paste': 'Coller',
            'notepad.edit.selectall': 'Tout sélectionner',
            'notepad.edit.find': 'Rechercher...',
            'notepad.format.wordwrap': 'Retour à la ligne automatique',
            'notepad.format.linenumbers': 'Afficher les numéros de ligne',
            'notepad.format.zoomin': 'Zoom avant',
            'notepad.format.zoomout': 'Zoom arrière',
            'notepad.format.zoomreset': 'Réinitialiser le zoom',
            'notepad.help.about': 'À propos de WebOSx',
            'notepad.find.placeholder': 'Rechercher dans le document...',
            'notepad.find.prev': 'Précédent',
            'notepad.find.next': 'Suivant',
            'notepad.find.noResults': 'Aucun résultat',
            'notepad.status.position': 'Lig {line}, Col {col}',
            'notepad.status.charOne': '{n} caractère',
            'notepad.status.charOther': '{n} caractères',
            'notepad.status.wordOne': '{n} mot',
            'notepad.status.wordOther': '{n} mots'
        });
        I18N.registerLang('en', {
            'app.notepad.placeholder': 'Type your text here...',
            'notepad.menu.file': 'File',
            'notepad.menu.edit': 'Edit',
            'notepad.menu.format': 'Format',
            'notepad.menu.help': 'Help',
            'notepad.file.new': 'New',
            'notepad.file.open': 'Open...',
            'notepad.file.save': 'Save as...',
            'notepad.edit.undo': 'Undo',
            'notepad.edit.redo': 'Redo',
            'notepad.edit.cut': 'Cut',
            'notepad.edit.copy': 'Copy',
            'notepad.edit.paste': 'Paste',
            'notepad.edit.selectall': 'Select all',
            'notepad.edit.find': 'Find...',
            'notepad.format.wordwrap': 'Word wrap',
            'notepad.format.linenumbers': 'Show line numbers',
            'notepad.format.zoomin': 'Zoom in',
            'notepad.format.zoomout': 'Zoom out',
            'notepad.format.zoomreset': 'Reset zoom',
            'notepad.help.about': 'About WebOSx',
            'notepad.find.placeholder': 'Search in document...',
            'notepad.find.prev': 'Previous',
            'notepad.find.next': 'Next',
            'notepad.find.noResults': 'No results',
            'notepad.status.position': 'Ln {line}, Col {col}',
            'notepad.status.charOne': '{n} character',
            'notepad.status.charOther': '{n} characters',
            'notepad.status.wordOne': '{n} word',
            'notepad.status.wordOther': '{n} words'
        });
        I18N.registerLang('es', {
            'app.notepad.placeholder': 'Escribe tu texto aquí...',
            'notepad.menu.file': 'Archivo',
            'notepad.menu.edit': 'Edición',
            'notepad.menu.format': 'Formato',
            'notepad.menu.help': 'Ayuda',
            'notepad.file.new': 'Nuevo',
            'notepad.file.open': 'Abrir...',
            'notepad.file.save': 'Guardar como...',
            'notepad.edit.undo': 'Deshacer',
            'notepad.edit.redo': 'Rehacer',
            'notepad.edit.cut': 'Cortar',
            'notepad.edit.copy': 'Copiar',
            'notepad.edit.paste': 'Pegar',
            'notepad.edit.selectall': 'Seleccionar todo',
            'notepad.edit.find': 'Buscar...',
            'notepad.format.wordwrap': 'Ajuste de línea',
            'notepad.format.linenumbers': 'Mostrar números de línea',
            'notepad.format.zoomin': 'Acercar',
            'notepad.format.zoomout': 'Alejar',
            'notepad.format.zoomreset': 'Restablecer zoom',
            'notepad.help.about': 'Acerca de WebOSx',
            'notepad.find.placeholder': 'Buscar en el documento...',
            'notepad.find.prev': 'Anterior',
            'notepad.find.next': 'Siguiente',
            'notepad.find.noResults': 'Sin resultados',
            'notepad.status.position': 'Lín {line}, Col {col}',
            'notepad.status.charOne': '{n} carácter',
            'notepad.status.charOther': '{n} caracteres',
            'notepad.status.wordOne': '{n} palabra',
            'notepad.status.wordOther': '{n} palabras'
        });
        I18N.registerLang('de', {
            'app.notepad.placeholder': 'Gib hier deinen Text ein...',
            'notepad.menu.file': 'Datei',
            'notepad.menu.edit': 'Bearbeiten',
            'notepad.menu.format': 'Format',
            'notepad.menu.help': 'Hilfe',
            'notepad.file.new': 'Neu',
            'notepad.file.open': 'Öffnen...',
            'notepad.file.save': 'Speichern unter...',
            'notepad.edit.undo': 'Rückgängig',
            'notepad.edit.redo': 'Wiederholen',
            'notepad.edit.cut': 'Ausschneiden',
            'notepad.edit.copy': 'Kopieren',
            'notepad.edit.paste': 'Einfügen',
            'notepad.edit.selectall': 'Alles auswählen',
            'notepad.edit.find': 'Suchen...',
            'notepad.format.wordwrap': 'Zeilenumbruch',
            'notepad.format.linenumbers': 'Zeilennummern anzeigen',
            'notepad.format.zoomin': 'Vergrößern',
            'notepad.format.zoomout': 'Verkleinern',
            'notepad.format.zoomreset': 'Zoom zurücksetzen',
            'notepad.help.about': 'Über WebOSx',
            'notepad.find.placeholder': 'Im Dokument suchen...',
            'notepad.find.prev': 'Vorherige',
            'notepad.find.next': 'Nächste',
            'notepad.find.noResults': 'Keine Ergebnisse',
            'notepad.status.position': 'Zeile {line}, Sp {col}',
            'notepad.status.charOne': '{n} Zeichen',
            'notepad.status.charOther': '{n} Zeichen',
            'notepad.status.wordOne': '{n} Wort',
            'notepad.status.wordOther': '{n} Wörter'
        });

        OS.registry.register({
            id: 'sys-notepad', name: I18N.t('app.notepad'), nameKey: 'app.notepad', icon: ICONS.notepad, defaultWidth: 640, defaultHeight: 480,
            init: (c, api) => {
                let am = null;
                c.innerHTML = `
                <div class="notepad-menu-bar">
                    <div class="notepad-menu-item" data-menu="file"><span data-i18n="notepad.menu.file">Fichier</span>
                        <div class="notepad-dropdown">
                            <div class="notepad-dropdown-item" data-action="new"><span class="notepad-check">✓</span><span data-i18n="notepad.file.new">Nouveau</span><span class="notepad-shortcut">Ctrl+N</span></div>
                            <div class="notepad-dropdown-item" data-action="open"><span class="notepad-check">✓</span><span data-i18n="notepad.file.open">Ouvrir...</span><span class="notepad-shortcut">Ctrl+O</span></div>
                            <div class="notepad-dropdown-item" data-action="save"><span class="notepad-check">✓</span><span data-i18n="notepad.file.save">Enregistrer sous...</span><span class="notepad-shortcut">Ctrl+S</span></div>
                            <div class="notepad-dropdown-sep"></div>
                            <div class="notepad-dropdown-item" data-action="close"><span class="notepad-check">✓</span><span data-i18n="chrome.win.close">Fermer</span></div>
                        </div>
                    </div>
                    <div class="notepad-menu-item" data-menu="edit"><span data-i18n="notepad.menu.edit">Édition</span>
                        <div class="notepad-dropdown">
                            <div class="notepad-dropdown-item" data-action="undo"><span class="notepad-check">✓</span><span data-i18n="notepad.edit.undo">Annuler</span><span class="notepad-shortcut">Ctrl+Z</span></div>
                            <div class="notepad-dropdown-item" data-action="redo"><span class="notepad-check">✓</span><span data-i18n="notepad.edit.redo">Rétablir</span><span class="notepad-shortcut">Ctrl+Y</span></div>
                            <div class="notepad-dropdown-sep"></div>
                            <div class="notepad-dropdown-item" data-action="cut"><span class="notepad-check">✓</span><span data-i18n="notepad.edit.cut">Couper</span><span class="notepad-shortcut">Ctrl+X</span></div>
                            <div class="notepad-dropdown-item" data-action="copy"><span class="notepad-check">✓</span><span data-i18n="notepad.edit.copy">Copier</span><span class="notepad-shortcut">Ctrl+C</span></div>
                            <div class="notepad-dropdown-item" data-action="paste"><span class="notepad-check">✓</span><span data-i18n="notepad.edit.paste">Coller</span><span class="notepad-shortcut">Ctrl+V</span></div>
                            <div class="notepad-dropdown-sep"></div>
                            <div class="notepad-dropdown-item" data-action="selectall"><span class="notepad-check">✓</span><span data-i18n="notepad.edit.selectall">Tout sélectionner</span><span class="notepad-shortcut">Ctrl+A</span></div>
                            <div class="notepad-dropdown-item" data-action="find"><span class="notepad-check">✓</span><span data-i18n="notepad.edit.find">Rechercher...</span><span class="notepad-shortcut">Ctrl+F</span></div>
                        </div>
                    </div>
                    <div class="notepad-menu-item" data-menu="format"><span data-i18n="notepad.menu.format">Format</span>
                        <div class="notepad-dropdown">
                            <div class="notepad-dropdown-item" data-action="wordwrap"><span class="notepad-check">✓</span><span data-i18n="notepad.format.wordwrap">Retour à la ligne automatique</span></div>
                            <div class="notepad-dropdown-item" data-action="linenumbers"><span class="notepad-check">✓</span><span data-i18n="notepad.format.linenumbers">Afficher les numéros de ligne</span></div>
                            <div class="notepad-dropdown-sep"></div>
                            <div class="notepad-dropdown-item" data-action="zoomin"><span class="notepad-check">✓</span><span data-i18n="notepad.format.zoomin">Zoom avant</span><span class="notepad-shortcut">Ctrl++</span></div>
                            <div class="notepad-dropdown-item" data-action="zoomout"><span class="notepad-check">✓</span><span data-i18n="notepad.format.zoomout">Zoom arrière</span><span class="notepad-shortcut">Ctrl+-</span></div>
                            <div class="notepad-dropdown-item" data-action="zoomreset"><span class="notepad-check">✓</span><span data-i18n="notepad.format.zoomreset">Réinitialiser le zoom</span><span class="notepad-shortcut">Ctrl+0</span></div>
                        </div>
                    </div>
                    <div class="notepad-menu-item" data-menu="help"><span data-i18n="notepad.menu.help">Aide</span>
                        <div class="notepad-dropdown">
                            <div class="notepad-dropdown-item" data-action="about"><span class="notepad-check">✓</span><span data-i18n="notepad.help.about">À propos de WebOSx</span></div>
                        </div>
                    </div>
                </div>
                <div class="notepad-findbar" id="notepad-findbar">
                    <input type="text" id="notepad-find-input" data-i18n-placeholder="notepad.find.placeholder" placeholder="Rechercher dans le document...">
                    <span class="notepad-find-count" id="notepad-find-count"></span>
                    <button class="notepad-find-btn" data-find-action="prev" data-i18n-title="notepad.find.prev" title="Précédent">˄</button>
                    <button class="notepad-find-btn" data-find-action="next" data-i18n-title="notepad.find.next" title="Suivant">˅</button>
                    <button class="notepad-find-btn close-find" data-find-action="close" data-i18n-title="chrome.win.close" title="Fermer">✕</button>
                </div>
                <div class="notepad-editor-wrap">
                    <div class="notepad-gutter" id="notepad-gutter"></div>
                    <textarea class="notepad-textarea" id="notepad-textarea" spellcheck="false" data-i18n-placeholder="app.notepad.placeholder" placeholder="${I18N.t('app.notepad.placeholder')}"></textarea>
                    <div class="notepad-mirror" id="notepad-mirror"></div>
                </div>
                <div class="notepad-status">
                    <span class="status-pos"></span>
                    <span id="notepad-charcount"></span>
                    <span id="notepad-wordcount"></span>
                    <span class="status-fill"></span>
                    <span id="notepad-zoom" class="dim">100 %</span>
                    <span class="dim">UTF-8</span>
                </div>`;

                // --- Références DOM ---
                const ta = c.querySelector('#notepad-textarea');
                const gutter = c.querySelector('#notepad-gutter');
                const mirror = c.querySelector('#notepad-mirror');
                const sp = c.querySelector('.status-pos');
                const charCountEl = c.querySelector('#notepad-charcount');
                const wordCountEl = c.querySelector('#notepad-wordcount');
                const zoomEl = c.querySelector('#notepad-zoom');
                const findBar = c.querySelector('#notepad-findbar');
                const findInput = c.querySelector('#notepad-find-input');
                const findCount = c.querySelector('#notepad-find-count');
                const fileInput = document.createElement('input');
                fileInput.type = 'file'; fileInput.accept = '.txt,text/plain'; fileInput.hidden = true;
                c.appendChild(fileInput);

                // --- État ---
                let wordWrap = true;
                let showLineNumbers = true;
                let fontSize = 14;
                let currentFileName = 'document.txt';
                let isModified = false;
                let rafPending = false;

                // --- Utilitaires ---
                function baseName(name) { return name.replace(/\.[^/.]+$/, ''); }
                function setModified(v) {
                    isModified = v;
                    api.setTitle(`${I18N.t('app.notepad')} — ${baseName(currentFileName)}${v ? ' •' : ''}`);
                }
                api.setTitle(`${I18N.t('app.notepad')} — ${baseName(currentFileName)}`);

                function closeAllMenus() {
                    if (am) { am.classList.remove('show'); am = null; }
                    c.querySelectorAll('.notepad-menu-item').forEach(m => m.classList.remove('active'));
                }

                // --- Gouttière / numéros de ligne ---
                function applyMirrorStyle() {
                    const cs = getComputedStyle(ta);
                    mirror.style.fontFamily = cs.fontFamily;
                    mirror.style.fontSize = cs.fontSize;
                    mirror.style.lineHeight = cs.lineHeight;
                    mirror.style.paddingLeft = cs.paddingLeft;
                    mirror.style.paddingRight = cs.paddingRight;
                    mirror.style.letterSpacing = cs.letterSpacing;
                    mirror.style.width = ta.clientWidth + 'px';
                }

                function getCurrentLineIndex() {
                    const before = ta.value.substring(0, ta.selectionStart);
                    return before.split('\n').length - 1;
                }

                function rebuildGutter() {
                    if (!showLineNumbers) { gutter.style.display = 'none'; gutter.innerHTML = ''; return; }
                    gutter.style.display = 'block';
                    const lines = ta.value.split('\n');
                    const fontSizePx = parseFloat(getComputedStyle(ta).fontSize);
                    const lineHeightPx = fontSizePx * 1.5;
                    gutter.style.fontSize = fontSizePx + 'px';
                    const curLine = getCurrentLineIndex();
                    let html = '';
                    if (wordWrap) {
                        applyMirrorStyle();
                        for (let i = 0; i < lines.length; i++) {
                            mirror.textContent = lines[i].length ? lines[i] : ' ';
                            const rows = Math.max(1, Math.round(mirror.scrollHeight / lineHeightPx));
                            html += `<div class="gutter-line${i === curLine ? ' current' : ''}" style="height:${rows * lineHeightPx}px">${i + 1}</div>`;
                        }
                    } else {
                        for (let i = 0; i < lines.length; i++) {
                            html += `<div class="gutter-line${i === curLine ? ' current' : ''}" style="height:${lineHeightPx}px">${i + 1}</div>`;
                        }
                    }
                    gutter.innerHTML = html;
                    gutter.scrollTop = ta.scrollTop;
                }

                function scheduleGutterRebuild() {
                    if (rafPending) return;
                    rafPending = true;
                    requestAnimationFrame(() => { rafPending = false; rebuildGutter(); });
                }

                // --- Barre d'état ---
                function updateStatus() {
                    const v = ta.value, pos = ta.selectionStart;
                    const before = v.substring(0, pos), lines = before.split('\n');
                    sp.textContent = I18N.t('notepad.status.position', { line: lines.length, col: lines[lines.length - 1].length + 1 });
                    charCountEl.textContent = I18N.t(v.length === 1 ? 'notepad.status.charOne' : 'notepad.status.charOther', { n: v.length });
                    const words = v.trim().length ? v.trim().split(/\s+/).length : 0;
                    wordCountEl.textContent = I18N.t(words === 1 ? 'notepad.status.wordOne' : 'notepad.status.wordOther', { n: words });
                }

                function refreshAll() {
                    updateStatus();
                    scheduleGutterRebuild();
                }

                ta.addEventListener('input', () => { setModified(true); refreshAll(); });
                ta.addEventListener('click', refreshAll);
                ta.addEventListener('keyup', refreshAll);
                ta.addEventListener('scroll', () => { gutter.scrollTop = ta.scrollTop; });
                ta.addEventListener('copy', () => { const sel = ta.value.substring(ta.selectionStart, ta.selectionEnd); if (sel) internalClipboard = sel; });
                ta.addEventListener('cut', () => { const sel = ta.value.substring(ta.selectionStart, ta.selectionEnd); if (sel) internalClipboard = sel; });
                new ResizeObserver(() => scheduleGutterRebuild()).observe(ta);

                // --- Zoom ---
                function applyZoom() {
                    ta.style.fontSize = fontSize + 'px';
                    zoomEl.textContent = Math.round((fontSize / 14) * 100) + ' %';
                    scheduleGutterRebuild();
                }
                function zoomIn() { fontSize = Math.min(28, fontSize + 2); applyZoom(); }
                function zoomOut() { fontSize = Math.max(10, fontSize - 2); applyZoom(); }
                function zoomReset() { fontSize = 14; applyZoom(); }

                // --- Retour à la ligne / numéros ---
                function applyWordWrap() {
                    ta.classList.toggle('nowrap', !wordWrap);
                    c.querySelector('[data-action="wordwrap"]').classList.toggle('checked', wordWrap);
                    scheduleGutterRebuild();
                }
                function applyLineNumbers() {
                    c.querySelector('[data-action="linenumbers"]').classList.toggle('checked', showLineNumbers);
                    scheduleGutterRebuild();
                }

                // --- Fichier ---
                function newFile() {
                    ta.value = ''; currentFileName = 'document.txt'; setModified(false); refreshAll();
                }
                function openFile() { fileInput.click(); }
                fileInput.onchange = () => {
                    const file = fileInput.files[0];
                    if (!file) return;
                    const reader = new FileReader();
                    reader.onload = () => {
                        ta.value = reader.result;
                        currentFileName = file.name;
                        setModified(false);
                        refreshAll();
                    };
                    reader.readAsText(file);
                    fileInput.value = '';
                };
                function saveFile() {
                    const blob = new Blob([ta.value], { type: 'text/plain' });
                    const a = document.createElement('a');
                    a.href = URL.createObjectURL(blob);
                    a.download = currentFileName || 'document.txt';
                    a.click();
                    URL.revokeObjectURL(a.href);
                    setModified(false);
                }

                // --- Édition ---
                let internalClipboard = '';
                function execEditCommand(cmd) {
                    ta.focus();
                    try { document.execCommand(cmd); } catch (e) { /* ignoré */ }
                    refreshAll();
                }
                function insertTextAtCursor(text) {
                    if (!text) return;
                    const start = ta.selectionStart, end = ta.selectionEnd;
                    ta.value = ta.value.substring(0, start) + text + ta.value.substring(end);
                    const newPos = start + text.length;
                    ta.focus();
                    ta.setSelectionRange(newPos, newPos);
                    setModified(true);
                    refreshAll();
                }
                function copyAction() {
                    const sel = ta.value.substring(ta.selectionStart, ta.selectionEnd);
                    if (sel) {
                        internalClipboard = sel;
                        if (navigator.clipboard && navigator.clipboard.writeText) navigator.clipboard.writeText(sel).catch(() => {});
                    }
                    ta.focus();
                }
                function cutAction() {
                    const start = ta.selectionStart, end = ta.selectionEnd;
                    const sel = ta.value.substring(start, end);
                    if (sel) {
                        internalClipboard = sel;
                        if (navigator.clipboard && navigator.clipboard.writeText) navigator.clipboard.writeText(sel).catch(() => {});
                        ta.value = ta.value.substring(0, start) + ta.value.substring(end);
                        ta.setSelectionRange(start, start);
                        setModified(true);
                    }
                    ta.focus();
                    refreshAll();
                }
                function pasteAction() {
                    ta.focus();
                    if (navigator.clipboard && navigator.clipboard.readText) {
                        navigator.clipboard.readText()
                            .then(text => insertTextAtCursor(text || internalClipboard))
                            .catch(() => insertTextAtCursor(internalClipboard));
                    } else {
                        insertTextAtCursor(internalClipboard);
                    }
                }

                // --- Recherche ---
                let lastMatches = [];
                function escapeRegExp(s) { return s.replace(/[.*+?^${}()|[\]\\]/g, '\\$&'); }
                function runSearch() {
                    const term = findInput.value;
                    if (!term) { lastMatches = []; findCount.textContent = ''; return; }
                    const re = new RegExp(escapeRegExp(term), 'gi');
                    lastMatches = [];
                    let m;
                    while ((m = re.exec(ta.value)) !== null) {
                        lastMatches.push(m.index);
                        if (m.index === re.lastIndex) re.lastIndex++;
                    }
                    findCount.textContent = lastMatches.length ? `0/${lastMatches.length}` : I18N.t('notepad.find.noResults');
                }
                function scrollToSelection() {
                    const fontSizePx = parseFloat(getComputedStyle(ta).fontSize);
                    const lineHeightPx = fontSizePx * 1.5;
                    const lineIdx = getCurrentLineIndex();
                    const targetTop = Math.max(0, lineIdx * lineHeightPx - ta.clientHeight / 2);
                    ta.scrollTop = targetTop;
                    gutter.scrollTop = targetTop;
                }
                function goToMatch(offset, term) {
                    ta.focus();
                    ta.setSelectionRange(offset, offset + term.length);
                    scrollToSelection();
                    refreshAll();
                }
                let findCursor = -1;
                function findNext() {
                    const term = findInput.value;
                    if (!term || lastMatches.length === 0) return;
                    findCursor = (findCursor + 1) % lastMatches.length;
                    findCount.textContent = `${findCursor + 1}/${lastMatches.length}`;
                    goToMatch(lastMatches[findCursor], term);
                }
                function findPrev() {
                    const term = findInput.value;
                    if (!term || lastMatches.length === 0) return;
                    findCursor = (findCursor - 1 + lastMatches.length) % lastMatches.length;
                    findCount.textContent = `${findCursor + 1}/${lastMatches.length}`;
                    goToMatch(lastMatches[findCursor], term);
                }
                function openFindBar() {
                    findBar.classList.add('show');
                    findInput.focus(); findInput.select();
                }
                function closeFindBar() {
                    findBar.classList.remove('show');
                    ta.focus();
                }
                findInput.oninput = () => { findCursor = -1; runSearch(); };
                findInput.addEventListener('keydown', (e) => {
                    if (e.key === 'Enter') { e.preventDefault(); e.shiftKey ? findPrev() : findNext(); }
                    else if (e.key === 'Escape') { closeFindBar(); }
                });
                c.querySelectorAll('[data-find-action]').forEach(btn => {
                    btn.onclick = () => {
                        const a = btn.dataset.findAction;
                        if (a === 'next') findNext();
                        else if (a === 'prev') findPrev();
                        else if (a === 'close') closeFindBar();
                    };
                });

                // --- Actions des menus ---
                const actions = {
                    new: newFile,
                    open: openFile,
                    save: saveFile,
                    close: () => api.close(),
                    undo: () => execEditCommand('undo'),
                    redo: () => execEditCommand('redo'),
                    cut: cutAction,
                    copy: copyAction,
                    paste: pasteAction,
                    selectall: () => { ta.focus(); ta.select(); },
                    find: openFindBar,
                    wordwrap: () => { wordWrap = !wordWrap; applyWordWrap(); },
                    linenumbers: () => { showLineNumbers = !showLineNumbers; applyLineNumbers(); },
                    zoomin: zoomIn,
                    zoomout: zoomOut,
                    zoomreset: zoomReset,
                    about: () => OS.launchApp('sys-about')
                };

                // --- Gestion des menus déroulants ---
                c.querySelectorAll('.notepad-menu-item').forEach(mi => {
                    mi.onclick = (e) => {
                        e.stopPropagation();
                        const dd = mi.querySelector('.notepad-dropdown');
                        if (am && am !== dd) { closeAllMenus(); }
                        if (dd.classList.contains('show')) { closeAllMenus(); }
                        else { dd.classList.add('show'); mi.classList.add('active'); am = dd; }
                    };
                });
                c.querySelectorAll('.notepad-dropdown-item').forEach(item => {
                    item.onclick = (e) => {
                        e.stopPropagation();
                        const action = item.dataset.action;
                        if (actions[action]) actions[action]();
                        closeAllMenus();
                    };
                });
                c.addEventListener('click', (e) => { if (!e.target.closest('.notepad-menu-item')) closeAllMenus(); });

                // --- Raccourcis clavier ---
                c.addEventListener('keydown', (e) => {
                    if (!e.ctrlKey) return;
                    const k = e.key.toLowerCase();
                    if (k === 's') { e.preventDefault(); saveFile(); }
                    else if (k === 'n') { e.preventDefault(); newFile(); }
                    else if (k === 'o') { e.preventDefault(); openFile(); }
                    else if (k === 'f') { e.preventDefault(); openFindBar(); }
                    else if (k === '+' || k === '=') { e.preventDefault(); zoomIn(); }
                    else if (k === '-') { e.preventDefault(); zoomOut(); }
                    else if (k === '0') { e.preventDefault(); zoomReset(); }
                });

                // --- Initialisation ---
                applyWordWrap();
                applyLineNumbers();
                applyZoom();
                refreshAll();

                // --- Localisation live (menus, findbar, statut, titre) si la langue change fenêtre ouverte ---
                function applyLocale() {
                    I18N.apply(c);
                    updateStatus();
                    runSearch();
                    setModified(isModified);
                }
                window.addEventListener('webosx:langchange', applyLocale);
                const origClose = api.close;
                api.close = () => { window.removeEventListener('webosx:langchange', applyLocale); origClose(); };
            }
        });
